<%= form_for([@image, @tag]) do |f| %>
  <% if @tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag.errors.count, "error") %> prohibited this tag from being saved:</h2>

      <ul>
      <% @tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% if @tag.new_record? %>
    <%= @image.tags.fields_for :tag do |tags_form| %>
      <%= tags_form.label :tag %>
      <%= tags_form.label :tag %><br>
    <% end %>
  <% else %>
    <div class="field">
      <%= f.label :Tag %><br>
      <%= f.text_field :str %>
    </div>
  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
